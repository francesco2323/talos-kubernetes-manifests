apiVersion: v1
kind: Service
metadata:
  name: traefik-dashboard
  namespace: traefik
  labels:
    app: traefik
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: dashboard
  selector:
    app: traefik
    